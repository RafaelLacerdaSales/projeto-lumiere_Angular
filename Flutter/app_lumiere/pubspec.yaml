name: app_lumiere
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Ícones estilo iOS
  cupertino_icons: ^1.0.8

  # Fonte Google (usada no layout dos cursos)
  google_fonts: ^6.2.1
  flutter_rating_bar: ^4.0.1   # <--- ADICIONE ESTA LINHA

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Ative se quiser usar imagens locais
  # assets:
  #   - assets/images/

  # Exemplo de como adicionar fontes locais (não obrigatório pois usamos google_fonts)
  # fonts:
  #   - family: CormorantGaramond
  #     fonts:
  #       - asset: fonts/CormorantGaramond-Regular.ttf
  #   - family: OpenSans
  #     fonts:
  #       - asset: fonts/OpenSans-Regular.ttf
